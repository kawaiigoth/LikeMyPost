<div class="container-fluid">
    <header class="row navbar-default">
        <div class="col-md-12">
            <div class="row background-mov">
                <div class="col-sm-11 col-sm-offset-1 col-xs-9"><a href="index"><span class="logo">LikeMyPost</span> </a> </div>
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#collapsedMenu">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
            </div>
            
        </div>
        <div class="container">
            <nav class="collapse navbar-collapse" id="collapsedMenu">
                <ul class="navigation  col-md-12">
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class=""> <a href="~/Add" >Добавить</a></li>
                    }                    
                    <li class=""> <a href="~/hot" id="hot">Горячее</a></li>
                    <li class=""> <a href="~/best" id="best">Лучшее</a></li>
                    <li class=""> <a href="~/new" id="new">Новое</a></li>
                    @if (User.IsInRole("Admin"))
                    {
                        <li class=""> <a href="~/Moderators" id="new">Администрирование</a></li>
                    }
                    @if (User.Identity.IsAuthenticated)
                    {
                        <li class=""> <a href="#" id="logout">Выйти</a></li>
                    }
                    else
                    {
                        <li class=""> <a href="#" id="login_show">Войти</a>
                    </li>
                    }
                </ul>
            </nav>
        </div>

    </header>
</div>